<UserControl x:Class="PokePrism.Pokemon.Views.PokedexView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <DataGrid ItemsSource="{Binding PokemonList, UpdateSourceTrigger=PropertyChanged}" />
        <Button Grid.Column="1" Command="{Binding TestCommand}" Content="show message" />
    </Grid>
</UserControl>
